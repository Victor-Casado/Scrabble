# Bot Starter Exploration

Branch snapshot of the evolved bot-starter exploration code. Token files, dependencies, build output, caches, logs, and oversized generated precompute tables are excluded.

