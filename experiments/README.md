# Opponent Modeling Experiments

This branch snapshot contains the Python opponent-modeling experiments used to
evaluate Lisan, Professor-style table overrides, MCMC precompute approaches,
and exploitability against modeled opponents.

The `precomputed/` files are retained because they are part of the experiment
record. Python cache output is excluded.

## Commands

```powershell
python -m pytest
```
