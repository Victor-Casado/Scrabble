# 1v1 and Stage-Cap Experiments

This branch snapshot contains the 1v1, 3-player, and stage-cap simulation
experiments used to decide the Atreides tournament pricing rules.

It includes saved result JSON/CSV files so the branch captures both the code and
the evidence used for the final model choices.

## Commands

```powershell
python -m pytest
```
